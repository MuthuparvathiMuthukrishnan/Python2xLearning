#Factorial using function
factor = int(input("Enter the value : "))
default = 1
def fact(*numbers):
    for default in numbers:
        return numbers
import math
result = fact(math.factorial(factor))
print("Factorial value of the given value : ", result)
#print(result)
#print(type(result))